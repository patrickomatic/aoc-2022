#lang racket

(require rackunit "day5.rkt")

;(test-case
  ;"test" (check-equal? (command 2 4 9) (command 2 4 9)))

;(test-case
  ;"parse-commands"
  ;(check-equal? (parse-commands '("" " 1  2  3  4" "move 2 from 4 to 9"))
                ;(list (move-command 2 4 9))))

; (test-case
;  "parse-stacks"
;  )

